{{ 'section-hero.css' | asset_url | stylesheet_tag }}

<section class="hero-section color-{{ section.settings.color_scheme }} gradient"
         id="Hero-{{ section.id }}"
         data-section="{{ section.id }}">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">{{ section.settings.hero_title | default: 'जोड़ों के दर्द से पाएं स्थायी राहत' }}</h1>
      <p class="hero-subtitle">{{ section.settings.hero_subtitle | default: "100% आयुर्वेदिक DIVJOT Pain Relief" }}</p>
      <p class="hero-benefit">{{ section.settings.hero_benefit | default: "7 दिन में दर्द से मुक्ति" }}</p>
      
      <div class="product-image">
        {% if section.settings.hero_product_image %}
          <img src="{{ section.settings.hero_product_image | img_url: '400x400' }}" alt="{{ section.settings.hero_title }}" loading="lazy">
        {% elsif settings.product_image %}
          <img src="{{ settings.product_image | img_url: '400x400' }}" alt="{{ settings.product_name }}" loading="lazy">
        {% else %}
          <img src="{{ 'product.png' | asset_url }}" alt="DIVJOT Pain Relief" loading="lazy">
        {% endif %}
      </div>
      
      <div class="pricing-section">
        <div class="price-box">
          <div class="special-price-label">विशेष मूल्य:</div>
          <div class="price">₹{{ section.settings.hero_price | default: 3150 }}</div>
          <div class="price-details">{{ section.settings.price_description | default: "180 टैबलेट (3 महीने का कोर्स)" }}</div>
          <div class="discount">{{ section.settings.discount_text | default: "70% की छूट - आज ही!" }}</div>
        </div>
        
        <div class="timer-box">
          <div class="timer-label">⏰ समाप्त होने में बचा समय:</div>
          <div class="timer" id="mainTimer">
            <div class="timer-unit">
              <span class="timer-number" id="mainHours">{{ settings.timer_hours | default: 2 | prepend: '0' | slice: -2, 2 }}</span>
              <span class="timer-text">घंटे</span>
            </div>
            <div class="timer-unit">
              <span class="timer-number" id="mainMinutes">{{ settings.timer_minutes | default: 47 | prepend: '0' | slice: -2, 2 }}</span>
              <span class="timer-text">मिनट</span>
            </div>
            <div class="timer-unit">
              <span class="timer-number" id="mainSeconds">00</span>
              <span class="timer-text">सेकंड</span>
            </div>
          </div>
        </div>
        
        <button class="cta-btn" onclick="openOrderModal()">अभी ऑर्डर करें</button>
      </div>
      
      <!-- Trust Indicators -->
      <div class="trust-indicators">
        <div class="trust-item">
          <span class="check-icon">✓</span>
          <span>कैश ऑन डिलीवरी</span>
        </div>
        <div class="trust-item">
          <span class="check-icon">✓</span>
          <span>तेज डिलीवरी</span>
        </div>
        <div class="trust-item">
          <span class="check-icon">✓</span>
          <span>100% सुरक्षित</span>
        </div>
        <div class="trust-item">
          <span class="check-icon">✓</span>
          <span>मनी बैक गारंटी</span>
        </div>
      </div>
      
      <button class="secondary-cta" onclick="openOrderModal()">🛒 अभी ऑर्डर करें - ₹{{ section.settings.hero_price | default: 3150 }}</button>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "Accent 1"
        },
        {
          "value": "accent-2", 
          "label": "Accent 2"
        },
        {
          "value": "background-1",
          "label": "Background 1"
        },
        {
          "value": "background-2",
          "label": "Background 2"
        }
      ],
      "default": "accent-1",
      "label": "Color scheme"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "जोड़ों के दर्द से पाएं स्थायी राहत"
    },
    {
      "type": "text",
      "id": "hero_subtitle",
      "label": "Hero Subtitle",
      "default": "100% आयुर्वेदिक DIVJOT Pain Relief"
    },
    {
      "type": "text",
      "id": "hero_benefit",
      "label": "Hero Benefit",
      "default": "7 दिन में दर्द से मुक्ति"
    },
    {
      "type": "image_picker",
      "id": "hero_product_image",
      "label": "Product Image"
    },
    {
      "type": "number",
      "id": "hero_price",
      "label": "Product Price",
      "default": 3150
    },
    {
      "type": "text",
      "id": "price_description",
      "label": "Price Description",
      "default": "180 टैबलेट (3 महीने का कोर्स)"
    },
    {
      "type": "text",
      "id": "discount_text",
      "label": "Discount Text",
      "default": "70% की छूट - आज ही!"
    },
    {
      "type": "checkbox",
      "id": "show_timer",
      "label": "Show countdown timer",
      "default": true
    },
    {
      "type": "range",
      "id": "timer_hours",
      "min": 0,
      "max": 24,
      "step": 1,
      "label": "Timer hours",
      "default": 2
    },
    {
      "type": "range",
      "id": "timer_minutes",
      "min": 0,
      "max": 59,
      "step": 1,
      "label": "Timer minutes",
      "default": 47
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %}